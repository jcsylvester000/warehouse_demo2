# dependencies
node_modules

# build output (Netlify rebuilds this)
dist
dist-ssr

# local / editor / OS
*.local
*.tmp
.DS_Store
.vite
.netlify

# vite temp config artifacts
vite.config.js.timestamp-*

# Internal supervisor documentation — not part of the app
Supervisor Docs/

# Backend secrets & generated client
.env
.env.*
!.env.example
prisma/dev.db
