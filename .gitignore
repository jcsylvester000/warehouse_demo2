# dependencies
node_modules

# build output (Netlify rebuilds this)
dist
dist-ssr

# local / editor / OS
*.local
*.tmp
.DS_Store
.vite
.netlify

# vite temp config artifacts
vite.config.js.timestamp-*
